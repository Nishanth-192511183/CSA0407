#include <stdio.h>
int main(){
    int index[20], n, i;
    printf("Enter number of file blocks: ");
    scanf("%d", &n);
    printf("Enter block numbers:\n");
    for(i = 0; i < n; i++)
    {
        printf("Block %d: ", i + 1);
    scanf("%d", &index[i]);}
printf("\nIndex Block\n");
printf("File Block\tBlock Number\n");
    for(i = 0; i < n; i++)
        printf("%d\t\t%d\n", i + 1, index[i]);
    printf("\nFile blocks are accessed using the index block.\n");
    return 0;
}
